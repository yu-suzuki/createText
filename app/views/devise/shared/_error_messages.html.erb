<% if resource.errors.any? %>
  <% unless resource.errors.full_messages.empty? %>
    <% resource.errors.full_messages.each do |e| %>
      <%= react_component("CustomizedSnackbars", {type: "warning", message: e}) %>
    <% end %>
  <% end %>
<% end %>
